[package]
name = "iroh-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "iroh_core"
# Android 通过 JNI 加载的共享库；产物名为 libiroh_core.so
crate-type = ["cdylib"]

[dependencies]
# JNI 绑定（与 Kotlin 侧 IrohCore.kt 对应）
jni = "0.21"
# iroh 点对点网络栈（含 discovery + relay，自动 NAT 穿透）
iroh = "1"
# 异步运行时（iroh 基于 tokio）
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "sync", "time"] }
serde_json = "1"
once_cell = "1"
anyhow = "1"
